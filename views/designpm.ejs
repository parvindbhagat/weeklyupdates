<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Project Manager: Projects</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
    <nav class="navbar">
      <a class="navbar-brand" href="/home">
        <img src="/images/logo.png" alt="Chrysalis" >
      </a>
    </nav>
    <div class=" menu d-flex flex-row-reverse gap-3 me-5 mt-2">
        <form style="display: inline-block; margin-right: 3rem;" action="/logout" method="POST">
            <button type="submit" class="btn rounded">Logout (<%= resourceName %>)</button>
        </form>
      <button> <a href="/home">Home</a> </button>  <br>
      <button><a href="/pwaactivities">Weekly Plan</a></button>
      <button><a href="/profile">My Tasks</a></button>
    </div>

    <h1 class="text-center mt-4">My Projects</h1>

    <% if (msg) { %>
        <div class="alert alert-warning alert-dismissible fade show" role="alert">
            <%= msg %>
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close">
        </div>    
    <% } %>


    <!-- create ol with projects and link to /designpm/:projectName for each client -->
    <% if (!projectsGroupedByClient.length) {%>
        <div class="container mt-4">
            <div class="alert alert-info" role="alert">
                You are not managing any projects currently.
            </div>
        </div>
    <% } else { %>
    <div class="container mt-4">
      <% projectsGroupedByClient.forEach(group => { %>
    <div class="card mb-3">
      <div class="card-header">
        <h5><%= group.clientName %></h5>
      </div>
      <div class="card-body">
        <ol class="list-group list-group-numbered mt-4">
          <% group.projects.forEach(project => { %>
             <li class="list-group-item">
               <a class="text-decoration-none" href="/designpm/<%= encodeURIComponent(project.interventionName) %>"><%= project.interventionName %></a>
               <p>Status: <%= project.ProjectStatus %></p>
                       <!-- Progress Bar -->
                <div class="progress " style="height: 20px;">
                  <div class="progress-bar 
                              <% if (project.ProjectPercentWorkCompleted < 50) { %> bg-danger 
                              <% } else if (project.ProjectPercentWorkCompleted < 80) { %> bg-warning 
                              <% } else { %> bg-success <% } %>" 
                       role="progressbar" 
                       style="width: <%= project.ProjectPercentWorkCompleted %>%;" 
                       aria-valuenow="<%= project.ProjectPercentWorkCompleted %>" 
                       aria-valuemin="0" 
                       aria-valuemax="100">
                    <%= project.ProjectPercentWorkCompleted %>%
                  </div>
                </div>
              </li>
          <% }) %>
        </ol>
      </div>
    </div>
    <% }) %>
    </div>
    <% } %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>